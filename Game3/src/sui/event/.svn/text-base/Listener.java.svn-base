package mdes.slick.sui.event;

import java.util.EventListener;

/**
 * A tagging interface which all SUI event listeners should extend.
 *
 * @author davedes
 * @since b.0.2
 */
public interface Listener extends EventListener {
}
